# Possible that this will grow.

type User {
  createdAt: ISO8601DateTime!
  email: String
  id: ID!
  image: String
  name: String
  nickname: String
  provider: String!
  uid: String!
  updatedAt: ISO8601DateTime!
}
